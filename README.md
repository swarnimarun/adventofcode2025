# Advent of Code 2025

To Run:
```bash
❯ LANG=rust DAY=day1 PART=part2 just run $LANG $DAY $PART "$PWD/.inputs/$DAY.input"
# assuming you store the inputs in .inputs folder
# note: the input path is an absolute path
```

Link to older advent of code 2024

## Rust language implementation notes

TODO
